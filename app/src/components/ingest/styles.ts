/** Shared inline style for form inputs in the ingest panel. */
export const inputStyle = {
  padding: "6px 8px",
  fontSize: 12,
  background: "var(--bg-tertiary)",
  color: "var(--text-primary)",
  border: "1px solid var(--border)",
  borderRadius: 4,
};
