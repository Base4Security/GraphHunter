//! v1 model surface — placeholder for the ADR-003 split.
//!
//! The active v1 feature extraction + inference code currently lives at
//! the crate root (`gnn_bridge`, `npu_scorer`). F4 will move it here
//! alongside the `common/` shapes and the experimental `v2/`.
